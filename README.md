# Refund Guardian

Refund Guardian is a blockchain-powered refund automation and loyalty token system.

## Setup
1. Install dependencies with `npm install`
2. Configure `.env`
3. Deploy contracts with Hardhat
4. Start backend with `node backend/server.js`
5. Start frontend with `npm run dev`
